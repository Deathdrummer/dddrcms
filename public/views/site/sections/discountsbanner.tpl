<section class="section discountsbanner"{% if data_scroll_id %} id="{{data_scroll_id}}"{% endif %}{% if data_scroll_block %} data-scroll-block="{{data_scroll_block}}"{% endif %}>
	<div class="container">
        <h2>
            <span>{{text_start}}</span>
            <span class="percent">{{text_percent}}</span>
            <span>{{text_end}}</span>
        </h2> 
    </div>
</section>